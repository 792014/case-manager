# المزامنة بين اللاب توب والموبايل عبر Firebase

التطبيق دلوقتي بيحفظ بياناته في قاعدة بيانات Firebase (Realtime Database) اللي أنشأتها، فبيشتغل من أي مكان عندك فيه إنترنت — مش لازم تكون على نفس الواي فاي، ومش لازم تسيب اللاب توب شغّال.

## اللي اتعمل بالظبط
التطبيق بقى بيقرا ويكتب البيانات مباشرة من/على قاعدة البيانات دي:
```
https://case-manager-32abd-default-rtdb.firebaseio.com/caseData
```
كل ما حد يضيف أو يعدّل قضية من أي جهاز (لاب توب أو موبايل)، البيانات بتتبعت لـ Firebase، وأي جهاز تاني فاتح التطبيق بياخد التحديث تلقائيًا (كل ٨ ثوانٍ، وفورًا لما ترجع تفتح التطبيق على الموبايل).

## خطوة مهمة: نقل بياناتك القديمة إلى Firebase
قاعدة بيانات Firebase حاليًا فاضية. عشان الـ 29 قضية والأحكام اللي كانت عندك (في النسخة الاحتياطية) تظهر على كل الأجهزة، لازم تستوردها مرة واحدة يدويًا:

1. افتح https://console.firebase.google.com واختار مشروعك (case-manager-32abd).
2. من القائمة الجانبية: **Realtime Database**.
3. جنب اسم قاعدة البيانات فوق، هتلاقي أيقونة ⋮ (ثلاث نقط) → اختار **Import JSON**.
4. اختار ملف `backup-seed.json` المرفق مع التطبيق.
5. **مهم جدًا**: البيانات لازم تتحط تحت مسار اسمه `caseData` بالظبط (مش في جذر القاعدة مباشرة). أسهل طريقة: أنشئ عقدة (node) اسمها `caseData` يدويًا الأول من داخل الواجهة، وبعدين استورد الملف جواها. لو مش واضحة الخطوة دي، ابعتلي وهساعدك بيها خطوة بخطوة.

بعد الاستيراد، افتح التطبيق (من اللاب توب أو الموبايل) وهتلاقي كل القضايا القديمة ظاهرة.

## تأمين قاعدة البيانات (مهم)
لو أنشأت المشروع في "وضع الاختبار" (Test Mode)، فقواعد Firebase بتسمح لأي حد معاه رابط القاعدة إنه يقرا ويكتب فيها لمدة ٣٠ يوم بس، وبعدها بتتقفل تلقائيًا. عشان تضمن إن بياناتك محمية بشكل دائم:

1. من **Realtime Database** → تبويب **Rules**.
2. حط القواعد دي كحد أدنى (تسمح بالقراءة/الكتابة من غير حماية قوية، بس أفضل من الفتح الكامل المؤقت):
```json
{
  "rules": {
    "caseData": {
      ".read": true,
      ".write": true
    }
  }
}
```
هذه القواعد لسه مفتوحة لأي حد معاه رابط القاعدة (زي ما بيحصل في نسخة تجريبية بسيطة). لو عايز حماية حقيقية (بحساب مستخدم/كلمة سر)، ده محتاج إضافة "Firebase Authentication" — ممكن نضيفها لاحقًا لو حبيت.

## ملحوظات
- مفتاح `apiKey` اللي في الإعدادات مش سر حساس (طبيعي يظهر في كود أي تطبيق Firebase من جهة العميل)، الحماية الحقيقية بتكون من الـ Rules فوق.
- التطبيق برضه بيحتفظ بنسخة محلية في المتصفح (localStorage) كـ "ذاكرة احتياطية" لو النت وقع لحظيًا، وبيرجع يزامن تلقائيًا لما النت يرجع.
- لو حابب تشتغل بدون إنترنت خالص على شبكة داخلية فقط، الملفات `server.js` و`package.json` المرفقة برضه شغّالة كخيار بديل (شرحها في نسخة سابقة من هذا الملف) — بس مش هي الطريقة المستخدمة دلوقتي.
